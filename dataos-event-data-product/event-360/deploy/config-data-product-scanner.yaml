name: wf-event-360-data-product-scanner
version: v1
type: workflow
description: |
  Scans and registers event-360 consumer-aligned data products and semantic models into the data catalog.
workflow:
  dag:
    - name: event-360-data-product-scanner
      description: |
        Scans the semantic model for event-360 and registers it into the data catalog.
      title: event-360 Data Product Scanner
      spec:
        stack: scanner:2.0
        compute: runnable-default
        runAsUser: metis
        stackSpec:
          type: semantic-model
          sourceConfig:
            config:
              type: SemanticModel
              markDeletedDataProducts: true
              dataProductFilterPattern:
                includes:
                  - event-360
